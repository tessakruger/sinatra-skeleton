<% @messages.each do |message| %>
	<div class="message">
		<h3><a href="/messages/<%= message.id %>"><%= message.title %></a> <small>(by: <%= message.author %>)</small>
		<p>
				<%= message.content %>
		</p>
	</div>
<% end %>
<hr/>
<a href="/messages/new">Post a message... It's FREE (for now)!</a>